{
  "name": "metallb-config-operator",
  "version": "0.0.4",
  "description": "Manage metallb config according specific test use case - bare metal cluster where internet facing nodes are placed in completely separate network segments",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lauris Vāvere",
  "license": "MIT",
  "repository": "vavere/metallb-config-operator",
  "dependencies": {
    "@kubernetes/client-node": "^0.11.0",
    "jsonpath": "^1.0.2"
  }
}
